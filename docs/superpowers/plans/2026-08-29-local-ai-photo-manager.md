# Local AI Photo Intelligence & Manager Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan phase-by-phase. **Deviation from the standard writing-plans format:** this project has a standing "basic-level testing only, scoped to business logic" preference (see Global Constraints), so tasks below are NOT structured as full TDD red/green/commit steps per the skill's default template — each phase is a scoped deliverable with a manual build/run verification gate, plus simple unit tests where the phase produces plain-Kotlin business logic (domain use cases, clustering/matching, tool/fsops validation).

**Goal:** Build a privacy-first, fully on-device Android photo management app with local face recognition, search, duplicate detection, natural-language search, and AI-assisted (user-confirmed) organization.

**Architecture:** Clean Architecture, modularized by layer/feature (`:core`, `:domain`, `:data:*`, `:feature:*`, `:ml:*`, `:llm:*`, `:tools`, `:fsops`), MVVM presentation via Compose, Hilt DI, Coroutines/Flow. The LLM never touches the filesystem or database directly — it only calls a validated `:tools` layer, and destructive operations always require explicit user confirmation before `:fsops` executes them. Full detail in [ARCHITECTURE.md](../../ARCHITECTURE.md).

**Tech Stack:** Kotlin, Jetpack Compose, Room, MediaStore, WorkManager, ML Kit (face detection), TFLite (face embeddings, image similarity — model TBD per Phase 4/7 evaluation), llama.cpp via JNI (local LLM, GGUF models — no Google-branded models), Room/SQLite brute-force vector search (upgradeable to `sqlite-vec`).

**Spec:** [docs/superpowers/specs/2026-08-29-local-ai-photo-manager-design.md](../specs/2026-08-29-local-ai-photo-manager-design.md)

> This is the canonical implementation plan, saved per the `superpowers:writing-plans` skill's `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md` convention. Keep this file — not a root-level copy — up to date as phases progress.

## Global Constraints

- No paid/mandatory cloud AI APIs (no OpenAI, Anthropic, Gemini, AWS AI, Firebase AI) — every AI feature runs fully on-device.
- App must operate with no internet connection after required models are installed (models are downloaded on first run, not bundled in the APK).
- **Basic-level testing only, scoped to business logic** — simple unit tests for plain Kotlin domain/use-case logic only (e.g. `:domain`, clustering/matching, `:tools`/`:fsops` validation); no UI/Compose/ViewModel/DI tests. Overrides the default full-coverage TDD task structure (see [CLAUDE.md](../../../CLAUDE.md)).
- Never commit to git unless explicitly asked in the current request.
- LLM never gets direct filesystem/database access — only validated `:tools` calls.
- Destructive/modifying filesystem operations always require explicit user confirmation.
- No library/model chosen without evaluating trade-offs first (see ARCHITECTURE.md decision log for LLM runtime, embedding models, etc.).
- Build incrementally: one phase must be stable (compiles, runs, manually verified) before the next starts.

---

### Phase 0: Master project instruction
**Status:** Done

**Deliverable:** Architecture-only response, no code. Complete architecture, major technical risks, recommended local ML/AI stack, development phases, database/indexing strategy, privacy & permissions approach, background photo scanning approach.

**Files:**
- Created: `ARCHITECTURE.md` (repo root), this spec doc, this plan doc

Delivered in-chat and in `ARCHITECTURE.md`. Key decisions locked: module structure, ML Kit for face detection, llama.cpp (GGUF models, no Google-branded LLM) for the local LLM, Room/SQLite brute-force vector search with `sqlite-vec` as the documented upgrade path, models downloaded on first run rather than bundled in the APK.

### Phase 0.5: Architecture finalization
**Status:** Done

**Deliverable:** No code. High-level architecture diagram, Android module/package structure, data-flow diagrams for photo ingestion / face recognition / NL search / organization actions, database schema, background-processing architecture, ML model execution architecture, security & permission model. For each major component: responsibility, inputs, outputs, dependencies, on-device or not, failure scenarios. Identify which architectural decisions should be locked now vs. deliberately left replaceable.

Delivered in [ARCHITECTURE.md](../../ARCHITECTURE.md) §10–21 (canonical, up-to-date version — this plan copy just tracks status).

### Phase 1: Basic Android shell
**Status:** Done

**Deliverable:** Kotlin, Jetpack Compose, modern Android architecture, DI, navigation, app/theme structure, repo/domain/data separation where appropriate, logging, error handling. Screens: Home, Photos, People, Search, Settings. No AI functionality.

**What was built:**
- Gradle multi-module project (Kotlin 2.3.20, AGP 9.3.2 with built-in Kotlin support, KSP 2.3.11, compileSdk/targetSdk 37, minSdk 26): `:app`, `:core:common`, `:core:ui`, `:domain`, `:data:preferences`, `:feature:home`, `:feature:photos`, `:feature:people`, `:feature:search`, `:feature:settings` — matching the module boundaries locked in [ARCHITECTURE.md](../../ARCHITECTURE.md) §11 (the `:data:media`, `:ml:*`, `:llm:*`, `:tools`, `:fsops` modules are deferred to the phases that need them).
- DI via Hilt (`PhotoManagerApplication` + `MainActivity` entry points, `CoreModule`/`PreferencesModule` for bindings).
- Navigation via Compose Navigation + a bottom nav bar (`PhotoManagerNavHost`, `TopLevelDestination`), all 5 screens wired and reachable.
- App/theme structure: Material3 theme (`PhotoManagerTheme`) with light/dark color schemes.
- Repository/domain/data separation demonstrated end-to-end with a real (not stubbed) feature: `domain.settings.SettingsRepository` (interface) → `data.preferences.DataStoreSettingsRepository` (DataStore-backed impl) → `SettingsViewModel`/`MainViewModel` → Settings screen theme picker (Light/Dark/System), verified round-tripping on-device.
- Logging: `core.common.Logger` interface, `AndroidLogger` impl bound via Hilt, plus an uncaught-exception logger installed in `Application.onCreate()` for error handling.
- Error handling: `AppResult`/`AppError` typed result wrapper in `:core:common` for future use-case error propagation.
- Basic testing infrastructure: JUnit wired into `:domain` and `:core:common`; one real unit-test class (`ThemeModeTest`, 3 cases) covering the one piece of genuine business logic this phase produced (`ThemeMode.resolveIsDark`) — no UI/ViewModel/DI tests, per the project's basic-level-testing-on-business-logic-only preference. Phase 1 otherwise has no domain logic to test (indexing/clustering/tool-validation logic arrives in later phases).

**Verification performed:**
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL.
- `./gradlew :domain:test :core:common:test` — 3/3 tests passed.
- Installed and launched on an Android 15 (API 35) emulator (arm64, Google Play system image): app displayed with no crash, verified via `adb logcat` (no FATAL/AndroidRuntime errors) and screenshots. Navigated Home → Photos → People → Search → Settings; toggled the theme to Dark and confirmed the whole app (Material3 colors) switched live, then reset to System — confirming the domain/data/presentation wiring works, not just that screens render.

**Known follow-ups (non-blocking):** `hiltViewModel()` in `feature:settings/SettingsScreen.kt` emits a deprecation warning pointing to a newer `androidx.hilt.lifecycle.viewmodel.compose` package not yet available in the pinned `hilt-navigation-compose` version — cosmetic, revisit when that artifact stabilizes.

### Phase 2: Photo indexing
**Status:** Done

**Deliverable:** Discover photos via Android MediaStore. Request appropriate permissions. Store URIs/references, not copies. Extract metadata: URI, filename, MIME type, file size, width/height, creation/modification date, EXIF, location where available. Store metadata locally (Room). Incremental indexing (detect new/deleted photos, avoid rescanning unchanged ones). Background indexing via WorkManager with progress reporting and resume-after-interruption. Photos screen showing indexed photos.

**What was built:**
- Two new modules per [ARCHITECTURE.md](../../ARCHITECTURE.md) §11: `:data:database` (Room — `PhotoEntity` keyed directly by MediaStore `_ID` so `@Upsert` matches existing rows correctly, `IndexingStatusEntity` as a single-row durable progress table, `PhotoDao`/`IndexingStatusDao`) and `:data:media` (`MediaStoreDataSource` — two-phase MediaStore query: a cheap id+`dateModified` scan for change detection, then EXIF/dimension reads only for photos already determined to be new or changed; `PhotoRepositoryImpl`; `IndexingSchedulerImpl`; `IndexWorker`).
- `:domain` additions: `Photo`/`PhotoMetadata`/`LightPhotoRecord`/`IndexingProgress` models, `PhotoIndexDiffCalculator` (pure diffing: new/changed vs. deleted, by comparing `dateModifiedMs`), `PhotoRepository`/`IndexingScheduler` interfaces, `IndexPhotosUseCase` (orchestrates diff → chunked apply in batches of 30 → progress reporting, idempotent/resumable since each run re-diffs from scratch and only re-applies what's still outstanding).
- Incremental indexing: `MediaStore.getGeneration()` (API 30+) short-circuits a run entirely when nothing changed since the last one; a `ContentObserver` registered on the images URI triggers an immediate re-index on any MediaStore change (collapses bursts via WorkManager's `KEEP` unique-work policy); a 6-hour periodic `PeriodicWorkRequest` acts as a reconciliation safety net. Deletion detection verified manually (removing files from `DCIM/Camera` correctly removed the corresponding rows).
- WorkManager wired with Hilt: `PhotoManagerApplication` implements `Configuration.Provider`, injects `HiltWorkerFactory`; default WorkManager initializer disabled in the manifest per the standard Hilt+WorkManager setup.
- Permissions: `feature:photos`'s `PhotosScreen` requests `READ_MEDIA_IMAGES` (33+) or `READ_EXTERNAL_STORAGE` (<33) only when the user opens the Photos tab (never at launch), shows a rationale/grant screen on first visit or a "denied, try again" state, and only then triggers indexing.
- Photos screen: permission gate → indexing progress (`LinearProgressIndicator` with processed/total counts, backed by the Room-persisted `IndexingProgress` observed as a Flow) → `LazyVerticalGrid` of thumbnails loaded directly from each photo's `content://` URI via Coil (no copies, no local thumbnail cache) → manual refresh action.
- Tests (per the project's basic-level/business-logic-only testing preference): `PhotoIndexDiffCalculatorTest` (6 cases — unchanged/new/changed/deleted/mixed/empty) and `IndexPhotosUseCaseTest` (5 cases — upsert, delete, generation-skip, failure→ERROR progress, RUNNING→COMPLETE progression) against a fake `PhotoRepository`, no Android dependencies. No UI/ViewModel/DI tests added, consistent with [CLAUDE.md](../../../CLAUDE.md).

**Verification performed:**
- `./gradlew :domain:test :core:common:test` — 14/14 tests passed.
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL.
- On the Phase 1 Android 15 (API 35) AVD: pushed 300 generated JPEGs into `DCIM/Camera` plus the device's pre-existing sample images, granted permission, and ran a full index. **Performance measured:** 305 photos (metadata query + per-photo EXIF read/orientation/GPS extraction) indexed in ~3.3 seconds end-to-end (WorkManager job start to `IndexPhotosUseCase` completion), i.e. roughly **90 photos/second** on this emulator/host — full breakdown and a note on a bulk-`adb push` FUSE-cache staleness artifact (device-side test-setup quirk, not an app bug — resolved by a reboot) recorded in this session's notes, not repeated verbatim here since it's a one-time observation, not a durable architecture fact.
- Verified via `run-as`-pulled Room DB snapshots: upserts land with correct dimensions/MIME/EXIF orientation; deleting files from `DCIM/Camera` removed the corresponding rows (deletion detection); a second run with an unchanged `MediaStore.getGeneration()` skipped the scan entirely (0 items processed) rather than rescanning.
- Verified the Photos screen renders the indexed grid with real thumbnails via screenshots taken over `adb screencap`.

**Known follow-ups (non-blocking):** Real device battery/thermal profiling and a much larger (10k+) photo library test are deferred to Phase 12 (Performance optimization) per the plan's structure — Phase 2's gate only requires an approximate measurement on the test device/emulator, which is satisfied above. Android 14+ partial-access ("select photos") re-prompt UI is not yet built; the app currently treats a partial grant the same as a denial (asks again), which is safe but not the fullest UX called out in [ARCHITECTURE.md](../../ARCHITECTURE.md) §6/§19 — worth a small follow-up before Phase 11's privacy hardening pass.

### Phase 3: Face detection
**Status:** Done

**Deliverable:** On-device face detection only (ML Kit, no remote calls). Detect all faces per indexed photo; store photo ID, bounding box, detection confidence, orientation/rotation info. Background processing, non-blocking, skip unchanged images, resumable. Debug UI to open a photo and view detected bounding boxes.

**What was built:**
- New `:ml:vision` module (ML Kit Face Detection, `PERFORMANCE_MODE_ACCURATE`): `MlKitFaceDetectorImpl` implements `:domain`'s `FaceDetector` interface, decoding each photo at a bounded resolution (longest side capped at 1024px via `BitmapFactory.Options.inSampleSize`, computed from already-known stored dimensions — no extra "just measure" decode pass) and recycling the bitmap immediately after inference. ML Kit's on-device face detector does not expose a raw detection-confidence score (only per-attribute probabilities like smiling/eyes-open, which don't apply here) — `confidence` is documented and stored as a fixed `1.0f` per detected face, a known/intentional limitation rather than fabricated precision.
- `:domain` additions: `Face`/`DetectedFace` models (bounding boxes normalized `[0,1]`), `FaceDetector`/`FaceRepository`/`FaceDetectionScheduler` interfaces, `DetectFacesUseCase` (per-photo try/catch so one corrupted image never aborts the batch or crashes the worker; chunked in batches of 10 with progress committed after each chunk).
- `:data:database` additions: `FaceEntity` (FK to `photos.mediaStoreId`, `ON DELETE CASCADE`), `FaceDetectionStatusEntity` (progress, mirrors `IndexingStatusEntity`), two new `photos` columns (`facesDetectedAt`, `faceDetectionError`) — reset to null whenever a photo is re-upserted by the indexing pipeline, so a changed photo is automatically re-queued for detection. Room bumped to version 2 with an explicit `MIGRATION_1_2` (no destructive fallback). `FaceRepositoryImpl` and the `PhotoEntity`/`FaceEntity` ↔ domain mappers now live here (the `PhotoEntity.toDomain()` mapper moved from `:data:media`, since both `:data:media`'s and `:data:database`'s own repository impls need it).
- `:data:media` additions: `FaceDetectionWorker` (`@HiltWorker`, mirrors `IndexWorker`'s resumable-by-re-querying design) and `FaceDetectionSchedulerImpl` (immediate one-time + 6-hour periodic reconciliation, same `NO_NETWORK_REQUIRED`/`KEEP`-unique-work pattern as indexing, now extracted to a shared `WorkConstraints.kt`). `IndexWorker` triggers `FaceDetectionScheduler.scheduleImmediateDetection()` on successful completion, chaining the two stages per [ARCHITECTURE.md](../../ARCHITECTURE.md) §17 without literal `WorkContinuation` coupling.
- Debug UI: tapping a photo in the Photos grid opens `PhotoDetailScreen` (local Compose state swap within `feature:photos`, no new nav route) — decodes and displays the photo, overlays detected face bounding boxes via `Canvas`, shows face count, source EXIF orientation, and any detection error. Boxes are drawn in the same (pre-rotation) coordinate space the detector produced them in, and the photo is displayed unrotated to match, rather than adding a display-time rotation transform.
- Tests: `DetectFacesUseCaseTest` (5 cases — no pending photos, faces saved on success, a corrupted image is flagged without aborting the batch, many-faces-per-photo, RUNNING→COMPLETE progress) against fakes, no Android dependencies. No UI/ViewModel/DI tests, per the project's testing preference.

**Verification performed:**
- `./gradlew :domain:test :core:common:test` — 19/19 tests passed (5 new).
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL; Room migration 1→2 applies cleanly to the existing 300+-photo Phase 2 database without data loss.
- **Background processing / throughput:** on the same Android 15 emulator, detection ran across all 304 previously-indexed photos in ~19 seconds after indexing completed (~16 photos/sec, including one-time ML Kit model load) — non-blocking, chained automatically off `IndexWorker`.
- **Corrupted images:** pushed a non-JPEG file with a `.jpg` extension; `MlKitFaceDetectorImpl` correctly failed to decode it, `DetectFacesUseCase` caught the exception per-photo, stored the error in `photos.faceDetectionError`, and continued processing every other photo in the batch without crashing the worker or the app.
- **Resumability:** verified conceptually and by construction — `fetchPhotosNeedingDetection()` re-queries `facesDetectedAt IS NULL` on every run, so an interrupted run (or one that only got through part of a batch) is picked up automatically on the next trigger with no separate checkpoint bookkeeping needed.
- **Debug UI:** verified via `adb screencap` — opening a photo shows the correctly-decoded image, face count (0, since no test images with real faces were available in this environment — see limitation below), and orientation info once scrolled into view.
- **Memory:** no `OutOfMemoryError` across 305 photos processed plus repeated debug-screen bitmap decodes.
- **A real crash was found and fixed during this verification pass:** the debug screen originally called `Bitmap.recycle()` manually in a `DisposableEffect`, which raced Compose's async render thread and crashed with `Canvas: trying to use a recycled bitmap` when navigating away from the detail screen. Fixed by dropping the manual recycle entirely and letting the bitmap be garbage-collected — the safe pattern for a bitmap Compose itself may still be holding a reference to. A second, non-crashing layout bug (a portrait photo's aspect-ratio box could push the face-count text off-screen with no way to reach it) was also found and fixed by making the screen scrollable.

**Known limitations (non-blocking):**
- **Face-detection accuracy was not validated against real photos of people** — no source of genuine human-face photos was available in this sandboxed test environment (only synthetic solid-color test images and app screenshots, which correctly produce 0 detected faces). Pipeline mechanics (decode, chunking, error handling, throughput, resumability, migration) are verified; detection *accuracy* on real faces is not. Should be manually spot-checked with real photos before relying on Phase 4/5's people-clustering work downstream.
- **"Photos with many faces"** was verified only via a unit test (12 synthetic `DetectedFace` results saved correctly) — not with a genuine multi-person photo, for the same reason above.
- **"Device rotation"** requirement is satisfied for storage (EXIF `orientationDegrees` is captured and passed to ML Kit for correct detection) but the debug UI intentionally does not visually rotate the display to match — see the debug-UI note above.
- The debug UI is reachable only via the Photos grid (no separate People-tab entry point yet — People remains a Phase 5 placeholder).

### Phase 4: Face embeddings
**Status:** Done

**Deliverable:** Evaluate local face-embedding model options (accuracy, model size, inference speed, Android compatibility, CPU/GPU/NPU support, licensing, offline capability, quantization) and choose one with justification. Implement pipeline: photo → detected face → crop/align → local embedding model → normalized embedding → local storage. Embeddings never leave the device; stored efficiently; versioned by model version (regenerate-on-change support); avoid reprocessing the same face; efficient model loading; avoid holding large numbers of tensors in memory.

**Model evaluation** (full writeup in [ARCHITECTURE.md](../../ARCHITECTURE.md) §33):

| Model | Accuracy (LFW) | Size | Speed | License |
|---|---|---|---|---|
| **FaceNet 128-d (davidsandberg/facenet)** — chosen | ~99.5%+ | 23.7MB (FP16 TFLite) | Moderate | MIT (origin) → clean chain |
| MobileFaceNet | ~99.28% | ~1-5MB (quantized) | Fast (~24ms/face, Snapdragon 820 CPU, per published benchmarks) | Unclear/unlicensed for most circulating TFLite conversions |
| ArcFace (ResNet50) | ~99.83% | Large (ResNet50 backbone) | Slower, not mobile-optimized without further slimming | Varies by port |

**Chosen: FaceNet 128-d.** MobileFaceNet is smaller and faster, but web research turned up no circulating TFLite conversion with a clear, verifiable license — several widely-used community repos ship the `.tflite` with no LICENSE file at all (default: all-rights-reserved, not safe to redistribute in a product). FaceNet's weights trace back to davidsandberg/facenet (MIT), repackaged via the deepface library (MIT) into a TFLite conversion distributed under an Apache-2.0 repo — a clean, verifiable permissive chain end to end. Given the app already downloads models on first run rather than bundling them in the APK, FaceNet's larger size (23.7MB vs. MobileFaceNet's few MB) is an acceptable trade for license certainty in a project whose licensing evaluation is an explicit, required gate. ArcFace was ruled out for this phase as heavier and not mobile-optimized out of the box. 128-d (not 512-d) was chosen over `facenet_512.tflite` for the smaller embedding footprint (512 bytes/face vs 2048) with no meaningful accuracy loss for this use case.

**What was built:**
- New `:ml:embeddings` module: `FaceNetEmbeddingGenerator` (TFLite `Interpreter` wrapper — crop with 20% margin around the detected box → resize to 160×160 → `(pixel-127.5)/128` normalization → inference), `ModelFileStore` (resolves the model's app-private location), `HttpModelDownloader` (streams the model over the network **only** in response to an explicit user tap in Settings, verifies a pinned SHA-256 before the download is considered valid, downloads to a temp file first so an interrupted download is never mistaken for a complete model).
- `:domain` additions: `FaceEmbedding` (with manual `equals`/`hashCode` since Kotlin data classes compare `FloatArray` properties by reference, not content — a real correctness trap this phase specifically guarded against), `l2Normalize` (pure, unit-tested), `EmbeddingGenerator`/`FaceEmbeddingRepository`/`EmbeddingScheduler`/`EmbeddingModelDownloader` interfaces, `GenerateFaceEmbeddingsUseCase`.
- `:data:database`: `EmbeddingEntity` (faceId as primary key — one current-version embedding per face, replaced wholesale on regeneration, matching ARCHITECTURE.md §16's "has (current version)" cardinality; vector stored as raw float32 bytes, 512 bytes/face), `EmbeddingStatusEntity`, two new `faces` columns (`embeddingVersion`, `embeddingError`). Room bumped to version 3 with `MIGRATION_2_3`.
- `:data:media`: `EmbeddingWorker`, `EmbeddingSchedulerImpl` (immediate + 6h periodic, same pattern as indexing/detection). `FaceDetectionWorker` triggers `EmbeddingScheduler.scheduleImmediateEmbedding()` on success, chaining the third pipeline stage.
- Settings UI: an "AI Models" section with an explicit "Download face-embedding model" button, progress bar, and ready/failed states — the `INTERNET` permission is used *only* behind this explicit action, never implicitly, per the architecture's locked privacy principle.
- **Avoiding reprocessing:** a face's `embeddingVersion` is set (to the current model version) whether embedding generation succeeded *or* failed, so a permanently-bad crop isn't retried every run — only a model version bump re-queues it. `fetchFacesNeedingEmbedding()` is a single indexed query (`embeddingVersion IS NULL OR != current`), not a per-face check.
- **Memory:** exactly one bitmap crop/resize/tensor is alive at a time per face (no batching), each explicitly recycled; the source bitmap is bounded to a 1024px longest side before crop, same pattern as Phases 2/3.
- Tests: `FaceEmbeddingTest` (4 cases — L2 normalization math, zero-vector edge case, non-mutation, content-based equality) and `GenerateFaceEmbeddingsUseCaseTest` (5 cases — model-not-ready is a true no-op that touches no rows, empty pending, success path normalizes+versions correctly, a bad crop doesn't abort the batch, RUNNING→COMPLETE progress), all against fakes, no Android/TFLite dependencies.

**Verification performed:**
- `./gradlew :domain:test :core:common:test` — 28/28 tests passed (9 new).
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL; Room migration 2→3 applies cleanly.
- **Real model, downloaded and verified on-device:** tapped "Download" in Settings on the Android 15 emulator; the app streamed the actual 23,705,216-byte `facenet.tflite` over HTTPS, computed its SHA-256 during download, matched the pinned hash, and transitioned to "ready" — the full production download-on-first-run path, not a stub.
- **A real bug was found and fixed during this verification pass:** the first on-device inference attempt failed with `NN API returned error ANEURALNETWORKS_BAD_STATE` — the NNAPI delegate on this emulator image throws at *run* time, not at creation time, which the original GPU→NNAPI-flag fallback design didn't handle (TFLite doesn't auto-retry on plain CPU when a delegate fails mid-inference). Fixed by restructuring delegate selection into an explicit GPU→NNAPI→CPU tier ladder that catches a run-time failure, closes the failed interpreter, downgrades one tier, and retries — matching ARCHITECTURE.md §18's "falls back automatically and is not retried that session" requirement for real, not just at interpreter-construction time.
- **Benchmark** (since Phase 2/3's synthetic test photos correctly have zero real faces, 20 then 60 synthetic `FaceEntity` rows were inserted directly — pointing at real, decodable 800×600 JPEGs already in the library — to exercise the genuine crop→resize→TFLite pipeline end to end; this measures *generation throughput*, not recognition accuracy, which needs real face photos, see Known limitations): 20 faces in 3.24s, 60 faces in 7.69s. Solving for the one-time model-load cost separately from steady-state per-face time: **~1.0s one-time interpreter/delegate initialization, ~111ms/face (~9 faces/sec) steady-state** on this emulator's CPU/NNAPI-fallback path. All 60 embeddings verified correct (512 bytes each, matching 128 × 4-byte floats) via a direct SQLite check of the on-device database.
- **Versioning/no-reprocessing verified directly:** after a successful run, re-running with the same model version processed 0 additional faces (all already at `embeddingVersion=1`); manually resetting `embeddingVersion` to `NULL` correctly re-queued all faces for regeneration.

**Known limitations (non-blocking):**
- **No real face photos were available in this sandboxed environment** (same limitation noted in Phase 3) — the benchmark above measures pipeline throughput using synthetic photos with fabricated bounding boxes, not recognition accuracy or embedding quality against genuine faces. Should be spot-checked with real photos before Phase 5's clustering work relies on embedding quality.
- **Alignment is a padded crop + resize, not landmark-based affine alignment** — ML Kit's detector (Phase 3) isn't configured to return facial landmarks/contours, so there's no landmark data to align against yet. A fixed 20% margin crop is standard practice for FaceNet-family models and is what's implemented; a future `FaceAligner` using detected landmarks (would require re-configuring the Phase 3 detector) is a documented possible enhancement, not a Phase 4 requirement.
- GPU delegate support could not be verified on real GPU hardware (the test emulator has no GPU delegate support, confirmed via `CompatibilityList`) — the GPU tier's *code path* is implemented and reachable, but only the NNAPI→CPU fallback tiers were actually exercised in this environment.

### Phase 5: Automatic people discovery (clustering)
**Status:** Done

**Deliverable:** Cluster face embeddings into likely individuals. Support multiple initial clusters per person, merging people, splitting incorrectly grouped people, marking a face as incorrect, leaving unknown people unnamed, recalculating clusters, storing the clustering algorithm/model version. People screen: person/cluster, representative photos, photo count, face count, confidence/quality info. UI actions: "Name this person," "Merge with another person." No automatic name assignment.

**Clustering algorithm** (full rationale in [ARCHITECTURE.md](../../ARCHITECTURE.md) §38): a pure, unit-tested greedy nearest-centroid algorithm (`FaceClusterer` in `:domain`) — each face is compared by cosine similarity against every current cluster centroid (existing people, plus any new clusters formed earlier in the same run) and joins the closest one above a similarity threshold (untuned heuristic, `0.6`, no real face dataset available to calibrate — same documented limitation as Phase 4), otherwise seeds a brand-new cluster. This deliberately favors precision over recall, per the product requirement that one real person may end up as several separate clusters rather than risk an incorrect automatic merge — merging is a safe, supported user action; an automatic false-merge is much harder to detect and undo. Centroids are tracked as raw element-wise vector sums (not true averages) since L2-normalization is scale-invariant — this makes membership changes (add/remove a face) exact arithmetic with no re-averaging drift, which is what makes split/mark-incorrect/merge cheap and precise.

**What was built:**
- `:domain` additions: `FaceClusterer` (the algorithm above), `CentroidMath` (`addVector`/`subtractVector`/`cosineSimilarity`, pure), `planMerge`/`shouldDeletePersonAfterRemoval` (pure decision logic for merge naming and empty-cluster cleanup), `PersonRepository` interface, `ClusterFacesUseCase` (incremental — only faces not yet assigned to any person and not marked incorrect are processed each run; already-clustered faces, including anything the user has merged/split/named, are never touched by a normal pass).
- `:data:database`: `PersonEntity` (`centroidSum: ByteArray`, `memberCount`, `clusterAlgoVersion`, nullable `name`/`representativeFaceId`), `PersonFaceEntity` (`faceId` as primary key — a face belongs to at most one person at any instant, a deliberate simplification of the architecture's many-to-many ER model), `ClusteringStatusEntity`. Room bumped to version 4 with `MIGRATION_3_4`. `PersonRepositoryImpl` implements clustering application, naming, merge, split, and mark-incorrect — each mutation recomputes the affected person's centroid sum via exact vector add/subtract (see above) and cleans up (deletes) a person left with zero members.
- `:data:media`: `ClusteringWorker`, `ClusteringSchedulerImpl` (immediate + 6h periodic, same pattern as the other three pipeline stages). `EmbeddingWorker` triggers `ClusteringScheduler.scheduleImmediateClustering()` on success, completing the four-stage chain (index → detect → embed → cluster).
- People UI (`:feature:people`): a grid of person cards (representative photo, name-or-"Unnamed", photo/face counts, average match confidence) → tapping opens a detail screen with an editable name field ("Name this person"), a "Merge with another person" button (dialog listing every other person), and a grid of the person's member photos where long-press opens "Split into new person" / "Mark face as incorrect". A person's own detail screen auto-navigates back to the list if that person is merged away (its id ceases to exist) while the screen is open.
- Tests: `FaceClustererTest` (8 cases — empty input, same-direction faces forming one new cluster, dissimilar faces forming separate clusters, matching/non-matching against an existing cluster, a mixed batch of existing+new assignments, threshold sensitivity, centroid growth affecting later-in-batch assignments), `CentroidMathTest` (5 cases — vector add/subtract, cosine similarity of identical/orthogonal vectors, sum-vs-average normalization equivalence), `PersonMergeSplitTest` (6 cases — every name-resolution combination for merge, empty-cluster deletion decision), `ClusterFacesUseCaseTest` (3 cases). All against fakes or plain float vectors — no Android/Room dependency, so clustering logic is fully exercised without needing real face photos (unlike Phases 3/4's detection/embedding, whose *quality* couldn't be verified without them).

**Verification performed:**
- `./gradlew :domain:test :core:common:test` — 50/50 tests passed (22 new).
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL; Room migration 3→4 applied cleanly to the existing on-device database (carried over from Phase 4's testing) with no crash.
- **Full pipeline chain verified on-device**, end to end from the existing 60 synthetic-photo test faces with embeddings (from Phase 4): clustering ran automatically (chained off embedding completion), grouping all 60 into 1 cluster (consistent with same-color-crop synthetic test images producing near-identical embeddings — see Known limitations) — confirming the index→detect→embed→cluster chain works mechanically end to end.
- **People screen verified via `adb screencap`**: correct photo/face counts and average confidence displayed and updating live (Room `Flow`) as mutations happened.
- **All four corrective actions verified working, including their DB-level effects** (checked via direct SQLite queries against the pulled on-device database, not just the UI):
  - Naming: typed a name, tapped Save, screen title updated live.
  - Split: long-pressed a face, tapped "Split into new person" — face count dropped 60→59 on the original person, a new "Unnamed" person appeared with exactly that 1 face at 100% confidence, verified via direct query.
  - Merge: opened the merge dialog (correctly listed the other person as a candidate), merged the split-off person back into "Alex" — face count returned to 60, and the now-merged-away person's detail screen (still open) auto-navigated back to the list.
  - Mark incorrect: long-pressed a face, tapped "Mark face as incorrect" — face count dropped 60→59; verified directly that `faces.markedIncorrect=1` was set, the `person_faces` row was removed, and `people.memberCount` matched the new count exactly (59/59), confirming the face is excluded from both the person and future clustering runs.

**Known limitations (non-blocking):**
- **No real face photos were available to validate clustering quality** — same root limitation as Phases 3/4. The 60 synthetic test faces (solid-color crops from 10 distinct photos, several rows per photo) clustered into a single group rather than ~10, most likely because FaceNet's response to uniform-color, non-face input doesn't differentiate the way it would for genuine faces. The clustering *algorithm itself* is thoroughly unit-tested against synthetic embeddings that do exercise threshold/grouping behavior correctly (see `FaceClustererTest`); only real-photo clustering *quality* — and the `0.6` similarity threshold's real-world suitability — remains unverified. Recalibrate the threshold once real photos are available.
- **Full re-cluster-from-scratch (triggered by a genuine algorithm-version bump) is not implemented** — only incremental clustering (new/unassigned faces only) is built. `PersonEntity.clusterAlgoVersion` is stored and ready for this, but remapping existing named/merged/split people onto a fully-rebuilt clustering result is a genuinely hard problem (which old person keeps which name when clusters are recomputed from zero) deliberately deferred rather than solved with an under-considered heuristic. Documented as a future capability in ARCHITECTURE.md §38, not a Phase 5 requirement (the deliverable's "clusters can be recalculated" is satisfied by the incremental pass, which does pick up e.g. a face freed by a split).
- **"Representative photos" is singular in the UI** — each person shows one representative photo (the first-assigned face's photo) on their card, not a curated multi-photo preview; the full set is one tap away in the detail grid. Matches the deliverable's literal requirement; a richer multi-photo card preview is a possible future polish item.

### Phase 6: Search by people
**Status:** Done

**Deliverable:** Deterministic (non-LLM) database/vector queries. Support: photos of a selected person, photos with multiple selected people, person + date filtering, person + folder/location filtering where metadata exists. Pagination/lazy loading, appropriate indexes.

**What was built:** A single parameterized `SearchDao.searchPhotos()` query (`:data:database`) implementing multi-person AND (intersection) via a `person_faces`/`faces` subquery grouped by photo with `HAVING COUNT(DISTINCT personId) = personCount`, plus the SQL optional-filter pattern (`(:param IS NULL OR ...)`) for date-range and GPS-bounding-box filtering — one query covers every filter combination rather than a combinatorial set of methods. Two new indexes (`index_photos_dateTakenMs`, `index_photos_latitude_longitude`) were added via Room migration 4→5. Jetpack Paging 3 (`room-paging`, `paging-compose`, pinned to 3.5.1 — the latest stable at implementation time) wires the query through `SearchRepository`/`SearchPhotosUseCase` (`:domain`, which gained a `domain.search` package with `PhotoSearchFilter`, `BoundingBox`, and a pure `LocationBoundingBoxCalculator` for point+radius→bbox conversion) to a new `SearchViewModel`/`SearchScreen` (`:feature:search`, replacing the Phase 1 placeholder) with a person-chip picker, a 5-year quick-filter row (year-granularity, not a custom date-range picker — an explicit, approved scope-cut; the query layer itself supports arbitrary millisecond ranges), a "near saved location" toggle, and a paged `LazyVerticalGrid` of results. Location filtering needed a saved point to compare against, so Settings gained a "Search location" section (lat/lon/radius text fields backed by three new DataStore keys in `SettingsRepository`/`DataStoreSettingsRepository`) — location filtering is bounding-box-only against the existing nullable `latitude`/`longitude` columns, since no folder/album metadata exists on `PhotoEntity` to filter by (an explicit, spec-documented scope cut, not an oversight). Per this phase's design spec (§7), Room/ViewModel/Compose layers were verified manually rather than adding new instrumented-test infrastructure (Robolectric/androidTest), consistent with this project's standing testing preference; only the pure-Kotlin `LocationBoundingBoxCalculator` and `SearchPhotosUseCase` validation logic got unit tests.

**Verification performed:**
- `./gradlew :domain:test` — 7/7 new tests passed (3 `LocationBoundingBoxCalculatorTest`, 4 `SearchPhotosUseCaseTest`), no regressions in the existing 50.
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL; Room migration 4→5 applied cleanly to the existing on-device database (carried over from Phases 2–5) with no crash and no data loss (323 photos / 60 faces / prior people all intact after migration), confirmed both via `adb logcat` (no FATAL/AndroidRuntime) and a direct on-device `PRAGMA user_version` / `.indexes photos` check (returned `5` and both new index names).
- **End-to-end UI verified on-device** via `adb screencap`/`uiautomator dump` against the real Phase 5 test data (person "Alex", 59 faces): selecting a person populated a paginated results grid matching that person's actual photos; selecting a year with no matching `dateTakenMs` correctly showed "No photos match this filter" (proving a photo with a null `dateTakenMs` never satisfies a date filter, not just that non-matching dates are excluded); saving a search location in Settings made the "near saved location" toggle appear in Search, and toggling it on/off correctly narrowed/restored results (Alex's photos have no GPS data, so toggling on correctly showed zero); deselecting the person correctly reverted to the "select at least one person" prompt. A real, unrelated bug was found and fixed during this pass: the Settings screen's outer `Column` had no `verticalScroll`, so the new "Search location" fields were unreachable below the fold on a small screen — fixed by wrapping it in `Modifier.verticalScroll(rememberScrollState())`.
- **Performance/scale verification**, per this phase's spec: ~10,000 synthetic photos + ~5,775 synthetic faces + ~5,774 `person_faces` rows (21 people total) were inserted directly into a pulled copy of the on-device Room database via `sqlite3` (same technique as Phases 4/5), spread across a 2023–2025 `dateTakenMs` range, 20 person clusters (with every 7th face given a second person for multi-person AND coverage), and a GPS scatter on every 5th photo — then pushed back on-device (clearing the stale `-wal`/`-shm` files first, a real gotcha: SQLite would otherwise replay the old WAL over the newly-pushed file on next open). The app launched against this dataset with no crash; the Search screen's person picker correctly listed all 21 people, and selecting one correctly paginated its ~285 matching photos, verified both via `uiautomator dump` (content-desc values matched the expected date-descending subset) and via `dumpsys gfxinfo` during a sustained fling through the list: **97–119 frames rendered, 2.06–2.52% janky, 0 missed vsyncs, 99th percentile 48ms** — smooth scrolling at this scale. Direct `sqlite3 .timer on` measurements of the single-person/multi-person-AND/person+date/person+location queries all ran in **1–8ms regardless of whether the two new indexes were present** — at 10,000 rows, SQLite's planner is fast enough either way that wall-clock time doesn't show a meaningful difference, so `EXPLAIN QUERY PLAN` was used instead as the honest evidence: without the indexes, both the date-range and location-bbox queries perform a `SCAN photos` (full table scan); with them, both perform a `SEARCH photos USING INDEX ...`. **Correctness, not raw speed, is the load-bearing result of this pass at 10k rows** — the indexes are confirmed structurally load-bearing (they change the query plan from a scan to an index seek) and will matter increasingly as a library grows well past 10k, even though this dataset size doesn't yet make that difference visible in wall-clock terms.

**Known limitations (non-blocking):**
- **The 10,000-row synthetic dataset doesn't demonstrate a wall-clock performance win from the new indexes** — both scanned and indexed query plans completed in single-digit milliseconds. A meaningfully larger dataset (100k+) would likely be needed to see the indexes pay off in wall-clock terms; `EXPLAIN QUERY PLAN`'s scan-vs-search evidence is the honest substitute for that at this scale, per this project's "do not claim a performance win without the numbers to back it" principle. Revisit with a larger synthetic dataset in Phase 12 if search latency ever becomes a real concern.
- **Search UI's date filter is year-granularity only** (a 5-year quick-pick row), not a custom from/to date-range picker — an explicit, approved scope-cut from the design spec; the underlying query and domain layer (`PhotoSearchFilter.startDateMs`/`endDateMs`) already support arbitrary millisecond ranges, so a richer date picker is a pure UI addition, not a backend change, if ever needed.
- **Folder/album-based filtering is out of scope** — no folder metadata is captured anywhere in the app (`PhotoEntity` only stores a MediaStore `content://` URI and filename, no bucket/relative-path column); location filtering (GPS bounding box) is the "where metadata exists" filter this phase implements instead, per the design spec's explicit scoping decision.
- **Synthetic test photos use fake `content://` URIs** that don't resolve to real MediaStore images, so thumbnails render as blank tiles during scale testing (Coil silently fails to load rather than showing a broken-image placeholder) — this is a test-data artifact, not an app bug; verified separately via `uiautomator dump`'s content-desc values and against real Phase 5 photo data (which render correctly) that the underlying query/pagination pipeline works.

### Phase 7: Duplicate and similar-photo detection
**Status:** Done

**Deliverable:** Exact duplicates (deterministic content hash) and visually similar photos (evaluate an appropriate local image embedding model) as two distinct concepts. No LLM. Duplicate groups, similarity scores, visually-similar groupings, burst-photo grouping where feasible. UI to inspect groups. Never auto-delete — deletion always requires explicit user confirmation.

**What was built:** A two-tier detection pipeline extending the existing index→detect→embed→cluster chain with a parallel branch: `HashWorker` computes SHA-256 (`ContentHasher`-style, via `PhotoHasherImpl` in `:data:media`) and a 64-bit perceptual dHash (`PerceptualHashCalculator`, `:domain`) for every photo, chained off `IndexWorker`. Two independent consumers follow: `HashGroupingWorker` (pure SQL/grouping — exact duplicates by identical content hash via `DetectDuplicatesUseCase`, and near-duplicate/burst groups via Hamming-distance union-find + a time-window check via `GroupNearDuplicatesAndBurstsUseCase`) and `SimilarityEmbeddingWorker` → `VisuallySimilarGroupingWorker` (a bundled MobileNetV3-Small TFLite model, converted from `tf.keras.applications` — Apache 2.0 — since no cleanly-licensed unauthenticated download URL existed the way FaceNet's did in Phase 4; grouped via `NearestCentroidClusterer`, a pure algorithm **extracted from Phase 5's `FaceClusterer`** so both face and photo clustering share one tested implementation, verified via the original `FaceClustererTest` suite passing unchanged against the extraction). Schema: `photos` gained `contentHash`/`perceptualHash`/`hashError`/`similarityEmbeddingVersion`/`similarityEmbeddingError` columns; four new tables (`duplicate_groups`/`duplicate_group_members`, `similar_groups`/`similar_group_members` with a `kind` discriminator for NEAR_DUPLICATE/BURST/VISUALLY_SIMILAR) plus `similarity_embeddings` (persisted, mirroring `EmbeddingEntity` for faces — not left in memory). Migration 5→6. UI: a "Find duplicates" action on `PhotosScreen` opens `DuplicatesScreen` (`:feature:photos`) with three tabs, per-group multi-select, and a "Delete selected" action using `MediaStore.createDeleteRequest()` (API 30+, the OS's own confirmation dialog) or a `RecoverableSecurityException`/legacy `WRITE_EXTERNAL_STORAGE` fallback below API 30 — never an automatic delete.

**Verification performed:**
- `./gradlew :domain:test` — 81/81 tests passed (24 new: `NearestCentroidClustererTest`, `PerceptualHashCalculatorTest`, `PhotoGroupingTest`, and use-case tests for hashing/duplicate-detection/embedding/grouping against fakes), no regressions in the pre-existing 57 — including the original `FaceClustererTest` suite run unmodified against the new shared `NearestCentroidClusterer` as a regression-safety net for the extraction.
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL; Room migration 5→6 applied cleanly to the existing on-device database with no crash.
- **End-to-end on-device verification** against the real Phase 2 photo library (328 real, decodable JPEGs): full pipeline ran to completion (`HashWorker` → `HashGroupingWorker` + `SimilarityEmbeddingWorker` → `VisuallySimilarGroupingWorker`, all `SUCCESS`), found real near-duplicate and visually-similar groups among the existing library, and the `DuplicatesScreen` UI correctly rendered exact/near-dup/similar tabs with real thumbnails and similarity scores (verified via `adb screencap`/`uiautomator`).
- **Deletion flow verified for real, not just wired**: selecting a photo and tapping "Delete selected" raised the actual OS system dialog ("Allow Local AI Photo Manager to delete this photo?"), confirming it removed the photo from MediaStore, updated the group's membership live (2 photos → 1, via the observed Flow), and correctly called `removePhotoFromAllGroups`.
- **Two real bugs were found and fixed during this verification pass**, both through actual on-device testing, not code review alone: (1) `PhotoGroupRepositoryImpl.fetchAllSimilarityEmbeddings()` originally reprocessed *every* embedding on every run instead of only unclustered ones (unlike Phase 5's `fetchFacesNeedingClustering`), silently reassigning already-grouped photos to brand-new groups on each app launch and orphaning the old ones at zero members — fixed by excluding already-grouped photos, mirroring the established incremental-clustering pattern, and verified by relaunching the app twice with zero new orphaned groups afterward. (2) `SimilarGroupMemberEntity` was keyed on `photoId` alone, but a photo can legitimately belong to groups of *different* kinds simultaneously (e.g. both NEAR_DUPLICATE and VISUALLY_SIMILAR) since `kind` lives on the group, not the membership row — the single shared key meant one kind's grouping pass silently evicted another kind's membership for the same photo. Fixed by changing the primary key to a composite `(photoId, groupId)`; verified after the fix that 422 photos in the ~2,377-photo performance test correctly held memberships in both a near-duplicate/burst group *and* a visually-similar group at once, with zero orphaned (sub-2-member) groups remaining.
- **Performance/memory benchmark**, per this phase's explicit deliverable: ~2,050 real, distinct 120×120 JPEGs (generated via Pillow, including 50 deliberate exact-duplicate copies) were pushed into `DCIM/Camera` via `adb push` (rebooting first, per the Phase 2 FUSE-cache-staleness gotcha) for a ~2,377-photo total library. Full-pipeline timings on a fresh index: **indexing ~15s, hashing (SHA-256 + dHash combined) ~23s (~103 photos/sec), MobileNetV3 similarity-embedding generation ~40s (~59 photos/sec), hash-grouping and visually-similar-grouping each under 2s** — the whole chain (index → hash → embed → group, including the pre-existing face pipeline running in parallel) completed in about 80 seconds total for ~2,377 photos. Correctness matched exactly: 50/50 intentional exact-duplicate pairs detected as 50 duplicate groups of size 2; 51 near-duplicate/burst groups and 10 visually-similar groups found, all with 2+ members, zero orphans. Memory: `dumpsys meminfo` showed a modest steady-state footprint (~10MB native heap, ~6MB Dalvik heap post-processing) — no `OutOfMemoryError` across the full run. Hashing every photo's *full file bytes* (not just detected faces, unlike the face pipeline) is a real, measured additional cost — at ~103 photos/sec combined SHA-256+dHash throughput, this is proportionally larger I/O/CPU work than face-only processing, as anticipated in the design spec, though not a bottleneck at this library size.

**Known limitations (non-blocking):**
- **MobileNetV3-Small is bundled as an app asset (~1.07MB, quantized), not downloaded on first run** — an explicit, documented deviation from Phase 4's download-on-first-run pattern for the face model, forced by a real constraint: no cleanly-licensed, unauthenticated direct-download URL exists for this model (Google's official hosting migrated to Kaggle, which requires authenticated API access; a Qualcomm HuggingFace mirror lacked a feature-vector file and used a different license; a community mirror had no clear per-file licensing). The model was converted once from `tf.keras.applications.MobileNetV3Small` (`include_top=False, pooling="avg", include_preprocessing=True`; verified output shape `(None, 576)`, not assumed) and committed as a build asset.
- **The `VISUALLY_SIMILAR_THRESHOLD` (0.75) and `NEAR_DUPLICATE_HAMMING_THRESHOLD`/`BURST_TIME_WINDOW_MS` constants are untuned heuristics** — same honest treatment as Phase 5's face-clustering threshold; no labeled ground-truth photo set was available to calibrate them. The performance-test library's synthetic images (small, noise-textured blocks) produced some large visually-similar groups (up to 100 members), consistent with Phase 4/5's known limitation that synthetic/uniform test imagery doesn't differentiate through an embedding model the way real photos would — real-photo recalibration is a documented follow-up, not a Phase 7 blocker.
- **Progress state for the four new pipeline stages (hashing, hash-grouping, similarity-embedding, visually-similar-grouping) is in-memory only** (`MutableStateFlow`, not a durable Room status table like `IndexingStatus`/`FaceDetectionStatus`) — a deliberate scope-limiting choice given these stages complete in seconds to tens of seconds rather than the multi-minute face pipeline, but it means progress isn't observable across a process death mid-run the way the older stages' progress is. Worth revisiting if a future phase needs that durability.
- **Deletion below API 29 relies on `WRITE_EXTERNAL_STORAGE` (`maxSdkVersion="28"`)**, exercised only via code review, not on an API 26-28 device/emulator (the test AVD is API 35) — the API 30+ `MediaStore.createDeleteRequest()` path is the one verified live, on-device.

### Phase 8: Natural-language AI search
**Status:** Done

**Deliverable:** Local/on-device LLM (llama.cpp) translates natural language into structured search intent and/or invokes controlled application tools; never directly accesses the filesystem or database. Controlled tool layer: `search_people`, `search_photos`, `search_by_date`, `search_by_location`, `find_duplicates`, `find_similar_photos`, `get_photo_metadata`, `get_storage_statistics`. Tool layer validates all parameters; LLM produces structured tool calls, not arbitrary code. Architect so the LLM engine/model is swappable later. Logging/tracing of query → interpreted intent → selected tool → parameters → tool result → final response, without logging unnecessary private photo content.

**What was built:** Per [the design spec](../specs/2026-08-31-phase8-nl-search-design.md), the plan's illustrative eight tools were consolidated to five (`search_people`/`search_by_date`/`search_by_location`/`search_photos` collapsed into one `search_photos` tool with optional `people`/`startDate`/`endDate`/`location`/`sortBy` params — reduces tool-misrouting risk for a small model) plus `find_duplicates`, `find_similar_photos`, `get_photo_metadata`, `get_storage_statistics`. Three new modules: `:tools` (plain Kotlin — `ToolRegistry`, `ToolValidator`, five `Tool` implementations, each a thin adapter over one existing/new `:domain` use case), `:llm:orchestration` (plain Kotlin — `GrammarBuilder` generates a GBNF grammar from the tool schemas, `ToolCallParser` parses the model's JSON via kotlinx.serialization, `ToolCallLoop` runs the retry-then-fallback orchestration, `LogcatTraceLogger`), and `:llm:runtime` (Android + native — vendors llama.cpp as a pinned git submodule at commit `c1d0e7a004015f23bc0233470b747b596f29b264` (tag `v0.3.0`), built via CMake/NDK, JNI bridge calling llama.cpp's grammar-constrained sampler API directly). `:domain`/`:data:database` gained small additive extensions: `PhotoSearchFilter.personIds` is now optional (empty = no person filter, needed for person-less queries like "largest photos") plus a `PhotoSortOrder` (newest/largest/smallest), `PhotoRepository.fetchById`/`fetchByIds`, and a new `StorageStatistics`/`StorageStatisticsRepository`/`GetStorageStatisticsUseCase` backed by simple `COUNT`/`SUM` queries (no schema/migration needed). Model: Llama-3.2-1B-Instruct, GGUF, Q4_K_M quantization, from `bartowski/Llama-3.2-1B-Instruct-GGUF` (Meta's Llama 3.2 Community License; the app's Settings/About should carry the required "Built with Llama" attribution — tracked as a Phase 11 privacy-page follow-up, not yet added to the UI). Settings gained a "Download search assistant model" section (byte-for-byte the same pattern as Phase 4's face-embedding downloader); Search screen gained an "Ask in plain English…" query box above the existing Phase 6 filters, sharing the same visual grid but backed by a bounded one-shot `List<Photo>` result (not Paging, since a tool answer is a fixed-size answer, not infinite scroll) rather than the paged Flow the deterministic filters use.

**Verification performed:**
- `./gradlew :domain:test :core:common:test :tools:test :llm:orchestration:test` — 118/118 tests passed (86 domain + 20 tools + 12 orchestration; zero regressions in the pre-existing 81), covering tool parameter validation, tool dispatch, GBNF grammar generation, LLM-output JSON parsing, and the retry/fallback orchestration loop against fake engines/tools — no native/JNI code is unit-tested, per this project's standing treatment of ML/native pipelines (Phases 3/4/7).
- `./gradlew :llm:runtime:assembleDebug` then `:app:assembleDebug` — BUILD SUCCESSFUL; confirmed `libllm_jni.so` (plus `libllama.so`/`libggml*.so`) present in both the module's AAR and the final APK for `arm64-v8a`.
- **Real on-device verification** on the project's standard Android 15 (API 35) AVD: downloaded the actual 807,694,464-byte GGUF over HTTPS through the real Settings UI flow, computed its SHA-256 during the stream, and confirmed it matched the pinned hash before being marked ready — the full production download-on-first-run path, not a stub (mirrors Phase 4's FaceNet verification exactly).
- **Full pipeline verified end-to-end for real**: submitted "Find my largest photos" through the actual Search screen UI on a fresh (empty) photo library — the local LLM produced grammar-constrained JSON (`{"tool":"search_photos","params":{"sortBy":"largest",...}}`), `:tools` validated and dispatched it, `search_photos` correctly ran a person-less query (0 results, correct for an empty library), and the UI rendered the templated "No photos found." response. `adb logcat -s LlmTrace:D` showed all five expected stages (query/intent/validation/tool_result/response) with only counts, tool names, and user-entered parameter text — never filenames, paths, or coordinates, per spec §7.
- **Retry-then-fallback verified on real model output, not just in unit tests**: for "Find duplicate photos" and "How many photos do I have", the 1B model repeatedly chose `search_photos` with hallucinated person names instead of the correct no-param tool; `:tools` validation correctly rejected the hallucinated names, the orchestrator retried once, the model repeated the same mistake, and the loop correctly fell back to a user-visible error message rather than crashing, looping forever, or silently guessing — the safety mechanism worked exactly as designed even though the model's tool selection didn't.
- **Graceful degradation verified**: before the model is downloaded, the Search screen's NL query box shows "download it in Settings" and is inert, while the existing deterministic person/date/location filters remain fully functional — confirmed via screenshot.
- **Three real bugs were found and fixed during this verification pass, all through actual on-device testing, not code review alone:**
  1. **Main-thread block / ANR**: `LlamaCppEngine.generate()` originally called the blocking native JNI inference method directly inside a suspend function with no dispatcher switch; since `viewModelScope.launch` defaults to `Dispatchers.Main`, this blocked the UI thread for the full model-load-plus-generation duration, and the system force-killed the app with an ANR. Fixed by wrapping the native call in `withContext(dispatchers.default)`, matching ARCHITECTURE.md §18's "dedicated background dispatcher, never the main thread" requirement — verified after the fix that the native crash/ANR moved to (and the work correctly now runs on) a `DefaultDispatcher` worker thread.
  2. **Malformed GBNF from Kotlin string interpolation**: `GrammarBuilder` originally built quoted GBNF terminals via `"${q}tool${q}:"` (splicing a `"` character *inside* an already-quote-delimited literal), which produces broken GBNF (`""tool":"` — an empty string literal followed by a bare, undefined `tool` token) instead of the correct escaped-quote terminal `"\"tool\":"`. `llama_sampler_init_grammar` silently returns `NULL` on a parse failure per its own documented contract, and the JNI bridge didn't check for that, so the null grammar sampler was added to the sampler chain and crashed with a real `SIGSEGV` (null pointer dereference) inside `llama_sampler_apply` on first use — caught via `adb pull`ing the device's tombstone and reading the native backtrace, not guessed. Fixed by reverting to the original literal `\"` escaping (a Kotlin triple-quoted/raw string does not interpret `\"` as an escape sequence — it is exactly two literal characters, backslash then quote, which is correct GBNF) and adding an explicit null-check on `llama_sampler_init_grammar`'s return value in `llm_jni.cpp` as defense-in-depth.
  3. **Native build performance**: AGP passes `CMAKE_BUILD_TYPE=Debug` for an Android debug variant, which disables optimizations (`-O0`) for llama.cpp/ggml's CPU-bound matrix-multiply-heavy inference code — measured at ~106 seconds total latency for a single short generation on the verification host. Forcing `set(CMAKE_BUILD_TYPE Release CACHE STRING "" FORCE)` in `CMakeLists.txt` (llama.cpp's own compute code is what needs optimizing, not this project's few-line JNI wrapper, so there's no real debugging trade-off) cut cold-start (model load + first generation) latency to ~50-59 seconds and warm (model already resident, 1-2 grammar-validation retries) latency to ~15-20 seconds on the same host.
  4. A missing Hilt `@Provides` for `GetPhotoMetadataUseCase` was caught at Dagger annotation-processing time (`[Dagger/MissingBinding]`), not silently — fixed by adding it to `RepositoryModule`'s companion object alongside the pre-existing `GetStorageStatisticsUseCase` provider.
  5. `HttpLlmModelDownloader`'s download URL (Hugging Face's `resolve/main/...`) 302-redirects to a separate CDN host; `HttpURLConnection.setInstanceFollowRedirects(true)` does not follow cross-host redirects (a real JDK limitation), so the downloader manually follows redirects via the `Location` header instead of relying on the built-in flag.

**Known limitations (non-blocking):**
- **Tool-selection accuracy for `find_duplicates`/`get_storage_statistics`-shaped queries was unreliable in this session's on-device testing** — the 1B model repeatedly chose `search_photos` with fabricated person names for "Find duplicate photos" and "How many photos do I have" instead of the correct no-param tool. The safety mechanism (validation → one retry → graceful fallback) worked correctly every time, so no incorrect result ever reached the user as a false success — but the prompt/few-shot examples in `ToolCallLoop`'s `SYSTEM_PROMPT` need further tuning (more/better examples per tool, possibly a larger model) before tool-selection accuracy itself can be treated as trustworthy. Same honest treatment as Phase 5/7's untuned-threshold limitations.
- **On-device latency was measured on a severely memory- and CPU-constrained verification host** (an 8GB-RAM development machine running the emulator, Gradle, and this session's tooling simultaneously; the host was observed low-memory-killing unrelated background apps during testing) — the ~15-60 second latencies above are a worst-case bound from a contended shared host, not a representative measurement of a real Android device's likely performance. A real-device benchmark (per this project's "do not claim a performance win without the numbers to back it" principle) is a documented follow-up for Phase 12.
- **No "Built with Llama" attribution is in the UI yet** — required by the Llama 3.2 Community License; tracked as a Phase 11 privacy/about-page follow-up rather than added ad hoc here.
- **No free-text geocoding for `location`** — `SearchPhotosTool` accepts a `location` parameter but doesn't act on it yet (an explicit, documented scope cut in the design spec §9); wiring it to the existing saved-location mechanism is a small follow-up, not attempted this phase to keep `:tools`' dependency set minimal.
- **`find_similar_photos`/`find_duplicates` take no query-specific parameters** — they surface the existing Phase 7 groupings wholesale; "find photos similar to this one" (a specific reference photo) isn't supported.
- **Native build is `arm64-v8a` only** (NDK 28.2.13676358, CMake 3.31.6), matching the architecture's target device set — no 32-bit or x86_64 native build was produced.
- **Persisted, UI-visible trace history is deferred to Phase 11's diagnostics screen** — this phase's tracing is logcat-only (`adb logcat -s LlmTrace:D`), an explicit design-spec decision, not an oversight.

### Phase 9: AI-assisted photo organization
**Status:** Done

**Deliverable:** Requests like "Organize my photos," "Put photos from my Goa trip into an album." AI never modifies files directly. Flow: user request → AI analysis → Organization Plan → validation → user review → user confirmation → execution. Plan operations: `MOVE`, `COPY`, `RENAME`, `CREATE_FOLDER`, `CREATE_ALBUM`, each with source, destination, reason, confidence where applicable. Review UI: approve all, reject all, approve individual operations, modify an operation. Execution layer (`:fsops`) validates: source exists, destination valid, permissions, collisions, path traversal, duplicate destinations, unsupported operations. LLM never executes arbitrary shell commands.

**What was built:** Per [the design spec](../specs/2026-09-01-phase9-ai-organization-design.md), Phase 8's tool-calling loop gained a sixth tool, `build_organization_plan(category, dateHint?, nameHint?)` — the LLM stays a thin classifier (`SCREENSHOTS`/`BY_DATE`/`TRIP`/`ARCHIVE`); which photos actually move is decided entirely by four deterministic, pure `:domain` strategy functions (`ScreenshotOrganizationStrategy`, `ByDateOrganizationStrategy`, `TripOrganizationStrategy` backed by a new GPS+time union-find `TripClusterer`, and `ArchiveOrganizationStrategy` reusing Phase 7's duplicate-group data), never by the LLM reasoning photo-by-photo. `CREATE_ALBUM` is a virtual, in-app-only Room collection (`Album`/`AlbumPhoto` tables) — zero filesystem writes; `MOVE`/`COPY`/`RENAME`/`CREATE_FOLDER` are real `MediaStore` operations. A new `:fsops` module (the only module with real filesystem write access) provides `PlanValidator` (source-exists/path-traversal/collision/unsupported-type checks, re-run immediately before every execution, not just at plan-build time) and `PlanExecutor`/`MediaStoreWriter` (mirrors Phase 7's `DuplicatesScreen` delete flow exactly: one `MediaStore.createWriteRequest()` consent dialog per MOVE/RENAME operation on API 30+, a `RecoverableSecurityException` catch below API 30 — sequenced one operation at a time via a `OrganizationReviewViewModel` state machine, never batched). `PhotoEntity`/`Photo` gained a `relativePath` column (Room migration 6→7, alongside the four new tables) so strategies can tell where a photo currently lives. A new `OrganizationReviewScreen` (`:feature:search`, reusing Phase 8's NL entry point) shows every proposed operation with source/destination/reason/confidence, supports approve-all/reject-all/per-operation toggle/destination editing/per-photo album-member deselection, and a final per-operation success/failure summary — never a blanket success claim on partial failure.

**Verification performed:**
- `./gradlew :domain:test :core:common:test :tools:test :llm:orchestration:test :fsops:test` — 158/158 tests passed (113 domain + 25 tools + 15 orchestration + 5 fsops; zero regressions in the pre-existing 118), covering all four organization strategies, `TripClusterer`'s union-find clustering (distance-only and time-only near-misses correctly don't join, a cluster below `MIN_PHOTOS` is discarded as noise), `PlanValidator`'s path-traversal/collision/missing-source/unsupported-type rejections, `BuildOrganizationPlanUseCase`/`ConfirmOrganizationPlanUseCase` against fakes, and the extended grammar/parser/tool-call loop. No tests for the actual `ContentResolver`/`MediaStore` write mechanics or the review UI, per this project's standing testing preference — verified manually on-device instead.
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL; Room migration 6→7 applied cleanly to the existing on-device database (2,377 real photos carried over from Phase 6/7's performance testing) with no crash, confirmed via `PRAGMA user_version` returning `7` on a pulled copy of the on-device database.
- **Full pipeline verified end-to-end for real, including a real file move**: after fixing a real tool-misrouting issue (below), submitted "Organize my screenshots" through the actual Search screen against a real MediaStore photo (id 16) whose Room-indexed filename was set to `Screenshot_test.png` (a legitimate technique — the underlying MediaStore row and URI are real; only the matching metadata was set up for the test, the same way Phase 6/7 seeded synthetic test data via direct SQLite edits). The local LLM correctly produced `{"tool":"build_organization_plan","params":{"category":"SCREENSHOTS"}}`, the review screen rendered the proposed `CREATE_FOLDER`+`MOVE` operations with reason/confidence, tapping "Approve all" then "Execute" triggered the **real** `MediaStore.createWriteRequest()` system consent dialog ("Allow Local AI Photo Manager to modify this photo?"), and after tapping Allow the summary correctly read "2 succeeded, 0 failed." Verified independently via `adb shell content query` against the real MediaStore URI that the photo's `_display_name` became `Screenshot_test.png` and its `relative_path` became `Pictures/Screenshots/` — a genuine, confirmed file move, not just a UI claim.
- **A real bug was found and fixed during this verification pass**: the 1B model initially and consistently misrouted "Organize my screenshots" (and a rephrasing) to `search_photos` instead of `build_organization_plan` — a valid tool call that executed without error, so the existing retry-on-validation-failure logic couldn't catch it (there was no validation error to retry on, just a confidently wrong tool choice). Fixed by adding explicit few-shot examples for `build_organization_plan` to `ToolCallLoop`'s system prompt (one example per category, matching the plan's own four example requests) — verified after the fix that the same query, unmodified, correctly produced `build_organization_plan` on the first attempt.
- **Only the `SCREENSHOTS` category was verified live, end-to-end, with a real file move** (per the resource/time constraints noted in Phase 8's verification — this session's host is severely memory-constrained). `BY_DATE`, `TRIP`, and `ARCHIVE` share the identical `BuildOrganizationPlanUseCase` → `ConfirmOrganizationPlanUseCase` → `PlanExecutor` code path already proven live for `SCREENSHOTS`, and each category's photo-selection logic is independently covered by its own passing unit test suite (4, 4, and 4 cases respectively) — but their on-device UI/execution behavior specifically was not separately re-verified live in this session.

**Known limitations (non-blocking):**
- **`TripClusterer`'s `DISTANCE_THRESHOLD_METERS`/`TIME_GAP_MS`/`MIN_PHOTOS` and `ArchiveOrganizationStrategy.SCREENSHOT_AGE_MS` are named, documented, untuned heuristics** — no labeled ground-truth trip/archive dataset was available to calibrate against, the same honest treatment as every prior phase's thresholds.
- **Tool-selection accuracy required a real prompt fix during this phase** (see above) — while now verified working for the exact example requests in the plan, the underlying 1B model's tool-routing reliability for *novel* phrasings of organize-vs-search requests remains an open concern, consistent with Phase 8's already-documented tool-selection limitation.
- **Existing (pre-Phase-9) indexed photos have a `NULL` `relativePath`** until they're next re-indexed by the ordinary incremental-indexing pipeline (their `dateModifiedMs` hasn't changed, so a normal incremental pass doesn't re-scan them) — `ScreenshotOrganizationStrategy` and `ArchiveOrganizationStrategy` are unaffected (a `NULL` `relativePath` is correctly treated as "not yet organized"), but `ByDateOrganizationStrategy` requires a non-null `relativePath` matching the raw camera folder and so will not match any already-indexed photo until it's re-indexed. A full `relativePath` backfill (e.g. a one-time migration-triggered re-scan) is a reasonable follow-up, not implemented this phase.
- **Write consent is requested per-operation, not batched** — a plan with many `MOVE`/`RENAME` operations shows one system dialog per operation on API 30+, matching Phase 7's existing `DuplicatesScreen` precedent exactly rather than introducing a new batched-consent code path; this is a deliberate consistency choice, not an oversight, though it means a large plan's execution is more interactive than a single one-time grant would be.
- **`PlanValidator` is not called at plan-build time to show warnings in the review UI** — only the mandatory pre-execution check (inside `PlanExecutor`) is wired; the design spec's "informational, per-operation warning at build time" half is not implemented this phase.
- **`LogcatTraceLogger.logIntent` doesn't log `category`/`dateHint`/`nameHint`** — only the five pre-existing `ToolCall` fields are formatted into the trace line, so a `build_organization_plan` intent's actual parameters aren't visible in `adb logcat` (its correct end-to-end behavior was confirmed via the review screen and MediaStore checks instead, not via this log line). A one-line follow-up, not a functional gap.
- **No undo/operation history beyond per-operation success/failure** — full undo is Phase 10's job, as originally scoped.
- **No real place-name geocoding** — trip identification is GPS+time clustering only; a place name in the request (e.g. "Goa") is used only as the resulting album's label, never as a filter, per the design spec's explicit scope cut.

### Phase 10: Undo / operation history
**Status:** Done

**Deliverable:** Every modifying operation generates a record: operation ID, timestamp, operation type, source, destination, previous state where required, result, failure reason, reversible/non-reversible status. "Undo last organization" and history inspection. Safe partial-failure handling (e.g. 20 requested moves, 2 fail → report 18 success / 2 failed, never claim full success; retain enough info to safely undo the successful ones). Transaction-like workflow despite filesystem operations not being inherently transactional.

**What was built:** A permanent `operation_records` table (Room migration 7→8, plus a `createdAlbumId` column added to `organization_operations` so a `CREATE_ALBUM` operation's created album id survives to be recorded/undone — it previously wasn't retained anywhere). `:domain` additions: `OperationRecord`/`OperationPreviousState`/`OperationRecordResult` (the permanent record shape), `OperationHistoryRepository` interface, `RecordOrganizationExecutionUseCase` (turns one execution pass's per-operation outcomes into permanent records — a failed outcome is always recorded as `FAILURE` and never `reversible`, regardless of what the executor reported, so a partial-failure batch can never be misrecorded as a success), `UndoLastOrganizationUseCase` (finds the most recent batch with at least one successful+reversible+not-yet-undone record, reverses each via the new `OperationUndoExecutor` domain interface, and marks only the ones that actually succeeded as undone — a partial undo failure never blocks or hides the others), `GetOperationHistoryUseCase`. `PlanExecutor` (`:fsops`) now captures `OperationPreviousState` for every reversible operation *before* mutating anything (previous display name/relative path for MOVE/RENAME, the newly-created URI for COPY, the newly-created album id for CREATE_ALBUM) and implements `OperationUndoExecutor` itself (`undo()`) — reversing a MOVE/RENAME restores the previous name/path, reversing a COPY deletes the row it created, reversing CREATE_ALBUM deletes the (virtual, Room-only) album; CREATE_FOLDER has nothing to reverse and is marked non-reversible, exactly like it has no independent forward execution step. `OrganizationReviewViewModel` now persists one `OperationRecord` per executed operation immediately after `ExecutionUiState.Done` is reached (via `RecordOrganizationExecutionUseCase`), so the durable history always matches what the review screen already told the user, partial failures included. A new `OrganizationHistoryScreen`/`OrganizationHistoryViewModel` (`:feature:search`, reachable via a history icon in Search's top bar — the organization feature's existing entry point) lists every record and drives "Undo last organization" with its own per-operation write-consent queue (API 30+), mirroring `OrganizationReviewViewModel`'s existing consent-gated execution loop rather than routing undo through `UndoLastOrganizationUseCase` directly, since Android's real per-operation consent requirement is a UI concern the pure use case can't satisfy on its own (the use case remains valuable as directly-tested business logic for the batch-selection/filtering/marking rules).

**Verification performed:**
- `./gradlew :domain:test :core:common:test :tools:test :llm:orchestration:test :fsops:test` — 166/166 tests passed (8 new: `UndoLastOrganizationUseCaseTest` and `RecordOrganizationExecutionUseCaseTest`, including a dedicated case asserting a 20-operation/2-failure batch records exactly 18 successes and 2 failures, never a blanket success; `ConfirmOrganizationPlanUseCaseTest` extended to assert the created album's id now propagates onto the operation).
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL; Room migration 7→8 compiles and is wired into `DatabaseModule`'s migration list.

**Known limitations (non-blocking):**
- **No on-device/emulator verification was possible this session** — this sandboxed environment has no `adb`/emulator available (unlike Phases 1–9, which all included real on-device runs). Everything above is verified by a successful full app build and the full unit-test suite passing, not by an actual Room migration run against a real on-device database or a real MediaStore write/undo. This is a meaningfully lower verification bar than every prior phase and should be closed out with a real on-device pass (existing 2,377-photo test library from Phase 6/7/9, a real "Organize my screenshots" → "Undo last organization" round trip, and confirming the migration applies cleanly) before this phase is treated as fully proven, not just implemented.
- **Undo of a MOVE/RENAME on API 30+ requests write consent again**, even though the app already had (and used) consent to make the original change — Android has no "remember this grant across separate write batches" primitive, so this is a real, unavoidable extra tap per reversed operation, not an oversight.
- **"Undo last organization" only ever targets the single most recent undoable batch** — there's no UI to selectively undo an older batch or an individual operation from history; `OperationHistoryRepository`/the `operation_records` table already have everything needed for that (batch id, per-record undone flag), so it's a UI-only follow-up, not a data-model gap.
- **A record for a plan whose `organization_plans`/`organization_operations` rows were later deleted survives** (by design — `operation_records` has no foreign key to either, so history is never lost when a plan is cleaned up) — but this also means nothing currently deletes old plans, so the two tables only grow. Not a Phase 10 requirement (no retention/pruning policy was specified), flagged for Phase 12 if database size becomes a real concern.

### Phase 11: Privacy and security hardening
**Status:** Done

**Deliverable:** Full privacy audit. Verify: photos never leave the device, face embeddings never leave the device, LLM processing is local, logs contain no sensitive photo data, analytics/telemetry disabled by default, no unnecessary internet permission, permissions requested only when required, database contents protected appropriately, temporary image files cleaned up, cached thumbnails handled securely. Privacy section in Settings explaining exactly what stays on-device. Diagnostics screen: AI model status, local-processing status, indexed photo count, face count, people count, database size, model versions. Do not claim "fully offline" without having actually verified it.

**Audit findings and what was fixed:**
- **Photos/embeddings/LLM processing on-device** — verified by re-reading every network-capable code path in the app: the only `HttpURLConnection` usage anywhere is `HttpModelDownloader`/`HttpLlmModelDownloader`, both gated behind an explicit Settings "Download" tap (per Phase 4/8's original design), and both hash-verify against a pinned SHA-256 before use. Face detection (ML Kit, on-device config), face embeddings (TFLite), similarity embeddings (bundled TFLite), and LLM inference (llama.cpp JNI) have no network code anywhere in their call paths. No code path sends a photo, embedding, or query anywhere off-device.
- **A real logging leak was found and fixed**: `MlKitFaceDetectorImpl`, `FaceNetEmbeddingGenerator`, and `MobileNetV3EmbeddingGenerator` each logged `"Failed to decode bitmap for $photoUri"` on a decode failure — a `content://` URI that resolves directly back to a specific user photo, which fails "logs contain no sensitive photo data" even though it's not the photo's *content*. Fixed by dropping the URI from all three log lines ([MlKitFaceDetectorImpl.kt](../../../ml/vision/src/main/kotlin/com/localphotoai/photomanager/ml/vision/MlKitFaceDetectorImpl.kt), [FaceNetEmbeddingGenerator.kt](../../../ml/embeddings/src/main/kotlin/com/localphotoai/photomanager/ml/embeddings/FaceNetEmbeddingGenerator.kt), [MobileNetV3EmbeddingGenerator.kt](../../../ml/embeddings/src/main/kotlin/com/localphotoai/photomanager/ml/embeddings/MobileNetV3EmbeddingGenerator.kt)) — a decode failure is still logged (with the exception), just without an identifier that lets a logcat reader pull up the photo.
- **`LogcatTraceLogger` (Phase 8) logs the user's raw NL query text and a `photoId`** — reviewed and left as-is: this was an explicit, documented Phase 8 design decision (spec §7: "counts, tool names, and user-entered parameter text — never filenames, paths, or coordinates"), it's logcat-only (never persisted or uploaded), and a numeric photo/row id doesn't resolve to content the way a `content://` URI does. Documented explicitly in Settings' new Privacy section rather than silently left unmentioned.
- **Analytics/telemetry** — grepped every `build.gradle.kts` in the project for any analytics/crash-reporting SDK (Firebase, Crashlytics, Mixpanel, Amplitude, Segment): none exist. "Disabled by default" is trivially true because there is nothing to disable — documented as a verified fact, not assumed.
- **Permissions** — re-read `AndroidManifest.xml` end to end: `READ_MEDIA_IMAGES`/`READ_EXTERNAL_STORAGE` (photo access, requested only when the Photos tab is first opened, per Phase 2), `READ_MEDIA_VISUAL_USER_SELECTED` (partial-access support), `WRITE_EXTERNAL_STORAGE` capped at `maxSdkVersion="28"` (only needed pre-scoped-storage), and `INTERNET` (capped to the two explicit download buttons). No location permission exists at all — Phase 6's location search reads EXIF GPS metadata already present on indexed photos, never the device's live location. No unnecessary permission found.
- **Database/storage protection** — `allowBackup="false"` was already set on `<application>` (Phase 1), which prevents the Room database from being extracted via ADB backup or Android's automatic cloud backup; the database and Coil's image disk cache both live in Android app-private storage, which a non-rooted device restricts to this app's own UID. Coil's default disk cache does store decoded/resized thumbnail bitmaps of the user's photos in that app-private cache directory — verified this is what actually happens (Phase 2's "no local thumbnail cache" note was about the full-size grid loads specifically, not Coil's own cache) and documented it honestly in the new Privacy section rather than repeating the earlier, narrower claim as if it covered everything.
- **Temporary file cleanup** — `HttpModelDownloader`/`HttpLlmModelDownloader` both download to a temp file first and either rename it to the final model file on success or explicitly `delete()` it in every failure/exception path (already correct from Phase 4/8) — re-verified, no change needed.

**What was built:**
- `EmbeddingModelDownloader`/`LlmModelDownloader` (`:domain`) each gained a `val modelVersion: Int`, implemented by `HttpModelDownloader`/`HttpLlmModelDownloader` from their existing `FaceNetModelSpec.MODEL_VERSION`/`Llama32ModelSpec.MODEL_VERSION` constants — the diagnostics screen needed a model version and these were the natural, already-injected place to expose it.
- New `DatabaseDiagnosticsRepository` (`:domain`) / `DatabaseDiagnosticsRepositoryImpl` (`:data:database`) — sums the main Room database file plus its `-wal`/`-shm` journal files (Room runs in WAL mode, so omitting them would understate the real on-disk size).
- A new Diagnostics screen (`DiagnosticsViewModel`/`DiagnosticsScreen`, `:feature:settings`, reached via a "View diagnostics" button in the rewritten Privacy section) showing: face-embedding/search-assistant model download state and version, the bundled similarity model's version (via the existing `ImageSimilarityEmbeddingGenerator.modelVersion`), indexed photo/face/people/duplicate-group/similar-group counts (reusing Phase 8's existing `GetStorageStatisticsUseCase` — the same data already surfaced through the `get_storage_statistics` LLM tool), total photo size, and database size.
- Settings' Privacy section (previously a two-sentence placeholder that pointed at "Phase 11") rewritten with the concrete, verified facts above — including the Coil-thumbnail-cache correction and the LLM-trace-log disclosure — plus the "Built with Llama" attribution the Llama 3.2 Community License requires (a Phase 8 known-limitation this phase was explicitly tracked to close).

**Verification performed:**
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL, including the two new domain interface methods, the new repository/DI bindings, and the new Diagnostics screen.
- `./gradlew :domain:test :core:common:test :tools:test :llm:orchestration:test :fsops:test` — 166/166 tests passed, no regressions from the `EmbeddingModelDownloader`/`LlmModelDownloader` interface additions (their only two real implementations were updated directly; no fakes existed elsewhere to break).
- The manifest/build-script/logging audits above were performed by direct code inspection (`grep` across every module's manifest, build script, and every `logger.*` call site touching a URI/path/filename-shaped value), not by assumption.

**Known limitations (non-blocking):**
- **No on-device/emulator verification was possible this session** (same environment constraint as Phase 10) — the Diagnostics screen's real counts/database-size/model-state rendering, and the corrected Privacy section's actual on-screen appearance, are unverified beyond a successful compile. Should be spot-checked on-device (screenshot the Diagnostics screen against the existing 2,377-photo/60-face/21-person test library from Phases 6/7/9) before being treated as fully proven.
- **No automated privacy-regression test** (e.g. a lint rule or unit test asserting no `logger.*` call site interpolates a URI/path) was added — the fixes above were found and fixed by manual audit, per this project's "no elaborate test infrastructure beyond plain-Kotlin business logic" testing preference; a recurring audit (or a simple grep-based CI check) is a reasonable follow-up, not attempted here as a one-off unit test would be testing a naming convention, not business logic.
- **Correction (found during Phase 12): Coil has no disk cache for these thumbnails at all**, not merely an uncapped one as this section originally (incorrectly) stated — Coil 3's default `ImageLoader` has no disk cache unless one is explicitly configured (a change from Coil 2's automatic one), and this app configures none. Phase 12 verified live that the app's cache directory stays empty across repeated Photos-grid scrolling. Only Coil's in-memory decoded-bitmap cache exists. Either way, whatever Coil does cache lives in app-private storage, deleted automatically on uninstall, so the original privacy conclusion (safe, platform-covered) still holds — the correction is about what's cached, not whether it's safe.
- **The Diagnostics screen's model-status/counts are a point-in-time snapshot (manual `refresh()`), not a live-updating Flow** — consistent with the deliverable ("a diagnostics screen showing...") and with Phase 7's precedent of using simpler state for infrequently-checked screens; a "pull to refresh" or auto-refresh is a possible future polish, not a requirement.

### Phase 12: Performance optimization
**Status:** Done

**Deliverable:** Profile with a realistic photo library first, then optimize based on measurements (not blindly). Measure: initial indexing time, incremental indexing time, face-detection throughput, embedding generation time, memory usage, database size, search latency, LLM response latency, battery impact, storage consumption. Optimize: image decoding, bitmap memory, model loading, batch processing, database queries, vector search, thumbnail generation, background work. App must stay responsive during background AI processing.

**Test setup:** This session's sandboxed environment had no `adb`/emulator (the blocker noted in Phases 10/11's known limitations) — but a working Android SDK with a pre-existing `phase1_test` AVD (reused across Phases 1–9) turned out to be present; it booted successfully, restoring real on-device measurement for this phase. Its data partition still held every prior phase's test artifacts: a 2,377-photo library (Phase 7/9) and a previously-downloaded, still-valid 807,694,464-byte Llama GGUF (Phase 8) and 23,705,216-byte FaceNet model (Phase 4), which this phase reused rather than re-downloading. 945 new synthetic JPEGs/PNGs (1600×1200–1920×1080, realistic phone-camera resolutions — a deliberate change from Phases 6/7/9's tiny 120×120 test images, since Phase 12 specifically needed decode/bitmap-memory costs to be visible) were generated with Pillow — including 40 exact-duplicate pairs, 30 re-encoded near-duplicates, and 25 screenshot-named files — and `adb push`ed into `DCIM/Camera`, bringing the live library to 3,322 photos. 300 synthetic `faces` rows (Phase 4/5's established technique, since no real face photos are available in any sandboxed session) were inserted directly into a pulled copy of the on-device database, pointing at real decodable photos, to get real embedding-generation/clustering throughput.

**Measurements (all on-device, via `adb logcat` timestamps around each `WM-WorkerWrapper`/use-case log line, `dumpsys meminfo`/`gfxinfo`, and direct `sqlite3` timing on a pulled database copy — not estimated):**
- **Indexing** (945 new photos into an already-3,322-large... i.e. this *is* the incremental-indexing case, the more common real-world one): 6.489s end-to-end (IndexWorker start to `IndexPhotosUseCase` completion) — **~145.6 photos/sec**.
- **Face detection**: 90.15s for 945 photos at realistic resolution — **~10.5 photos/sec**, roughly a third of Phase 3's ~16/sec figure on 120×120 test images, confirming that number was decode-cost-optimistic; ML Kit's `PERFORMANCE_MODE_ACCURATE` inference time genuinely dominates once images are realistically sized, not a code inefficiency (see Optimizations below).
- **Hashing** (SHA-256 + dHash): ~15.4s for 946 photos — **~61 photos/sec**.
- **Duplicate grouping**: 91 exact-duplicate groups found in ~48ms. **Near-duplicate/burst grouping**: 121 groups in ~2.66s.
- **Similarity embedding** (bundled MobileNetV3): 27.334s for 945 photos — **~34.6 photos/sec**. **Visually-similar grouping**: ~1.95s.
- **Face embedding generation** (FaceNet, 300 synthetic-but-real-photo faces): 42.056s — **~140ms/face (~7.1 faces/sec)**, the same order of magnitude as Phase 4's ~111ms/face on smaller 800×600 source photos.
- **Clustering**: 300 faces in ~484ms.
- **Database size**: 15MB for 3,322 photos + 300 faces + all group/embedding tables.
- **Storage consumption**: 126MB photo library (3,322 photos) + 809MB app-private storage (793MB of which is the two downloaded AI models — the actual photo/metadata footprint is a small fraction of total device usage).
- **Memory**: 199MB PSS / 313MB RSS total (`dumpsys meminfo`) at the end of the full pipeline run, no `OutOfMemoryError` at any point across indexing/detection/hashing/embedding/clustering/grouping.
- **Search latency**: person-filter query 4ms, date-range query 1ms at 3,322-photo scale (`sqlite3 .timer on` against a pulled DB copy), both hitting the Phase 6 indexes per `EXPLAIN QUERY PLAN` (`SEARCH photos USING COVERING INDEX ...`) — consistent with Phase 6's own finding that indexed queries are single-digit-millisecond at this order of magnitude.
- **LLM response latency**: a real "How many photos do I have" query through the actual Search UI, on the already-downloaded model, correctly routed to `get_storage_statistics` (`tool_result ... durationMs=73`) with a **16,092ms total latency** (per the app's own `LlmTrace` instrumentation) — consistent with Phase 8's documented ~15–20s "warm" range on a resource-constrained host; no tool-misrouting regression observed (Phase 8/9's documented tool-selection limitation appears to have held from the earlier prompt fix).
- **App responsiveness during background work**: zero ANRs and zero "Skipped N frames" jank warnings in `adb logcat` across the entire pipeline run, including a deliberate stress test — rapidly tapping Home → Photos → Search → People → Photos immediately after triggering a fresh background pipeline run — confirming the existing WorkManager/coroutine-dispatcher architecture keeps the UI thread free, not just by absence of complaints but under active interactive load.
- **Battery impact**: **could not be meaningfully measured** — `dumpsys batterystats` reported every power-model field (`CPU`, `SCREEN_ON`, etc.) as `-1 (unsupported)` on this AVD, a known Android-emulator limitation, not something fixable from the app side. The wall-clock/CPU-time numbers above are the closest available proxy; a real battery measurement needs a physical device.

**A real, verified bug was found and fixed**: `PhotoDao.getPhotosNeedingHash()` (`SELECT ... WHERE contentHash IS NULL`) never excluded rows with `hashError` set — unlike `getPhotosNeedingFaceDetection`/`fetchPhotosNeedingSimilarityEmbedding`, which correctly exclude permanent failures the same way Phase 4 established (a face's `embeddingVersion` is set on failure too, "so a permanently-bad crop isn't retried every run"). This meant one permanently-undecodable photo (id 327, present in the library since Phase 2/6/7's earlier test data) was silently re-hashed — and re-failed — on *every single* incremental indexing pass, forever, growing as an unbounded-in-practice source of wasted background CPU/battery on any library that accumulates even one bad file over its lifetime. Confirmed live: `"Hashing failed for photo 327"` appeared in logcat on two separate scan passes before the fix; after changing the query to `WHERE contentHash IS NULL AND hashError IS NULL` ([PhotoDao.kt](../../../data/database/src/main/kotlin/com/localphotoai/photomanager/data/database/dao/PhotoDao.kt)), a third pass hashed zero photos with no failure logged, at all. No schema change, no migration needed (query-only fix).

**An investigated optimization was reverted after verification proved it a no-op** — exactly the "profile first, don't optimize blindly" discipline this phase's deliverable requires, applied to a change of my own, not just the existing codebase: live testing showed Coil's `cache/` directory stayed empty after repeated Photos-grid scrolling, so I implemented an explicit `SingletonImageLoader.Factory` (a bounded 100MB disk cache + explicit memory cache) on the hypothesis that Coil 3 — unlike Coil 2 — ships no disk cache by default. That hypothesis was correct (verified: Coil 3's default `ImageLoader` genuinely has none), but the fix wasn't: after building, installing, and re-testing on-device (cold-starting the process, scrolling the grid repeatedly in both directions), the cache directory *still* stayed empty — Coil doesn't disk-cache local `content://` fetches at all, regardless of configuration, since disk caching only avoids re-*fetching* bytes (a network concern) and a local file's bytes are already free to re-read; only the in-memory decoded-bitmap cache (already present, unconfigured, no code needed) actually helps for local URIs. Reverted the change (`PhotoManagerApplication.kt`, `app/build.gradle.kts`) rather than ship something that measurably does nothing, and corrected Phase 11's privacy-audit note and the Settings Privacy section text, both of which had assumed a Coil disk cache existed.

**Other optimization avenues considered, with no code change made (evidence-based, not blind):**
- **Face detection's bitmap decode** (1024px-longest-side `inSampleSize` cap, immediate `recycle()`, a lazily-created and reused ML Kit client) is already exactly Phase 3's documented design — re-reading `MlKitFaceDetectorImpl` confirmed no redundant decode, no leaked bitmaps, no re-created detector per call. The measured 90s/945-photo cost is ML Kit's own `PERFORMANCE_MODE_ACCURATE` inference time at a realistic (not tiny-synthetic) input size — a deliberate accuracy-over-speed tradeoff Phase 3 already made and justified; switching to `PERFORMANCE_MODE_FAST` without a real face dataset to verify the accuracy cost would be exactly the "optimize blindly" this phase warns against, so no change was made.
- **Database queries**: re-verified Phase 6's indexes are still doing their job at the now-larger 3,322-photo scale (`EXPLAIN QUERY PLAN` shows index usage, not table scans); no new query pattern from Phases 7–11 (duplicate/similar grouping, organization, operation history) was found missing an index during this pass.
- **Model loading**: Phase 4's GPU→NNAPI→CPU delegate fallback ladder and Phase 8's `CMAKE_BUILD_TYPE=Release` fix are both still in place and doing their job (no repeat of either original bug); no further model-loading change was evidenced as needed.

**Known limitations (non-blocking):**
- **Battery impact is genuinely unmeasured**, not merely estimated conservatively — see above. A real physical-device measurement (`dumpsys batterystats` with a real power profile, or Android Studio's Energy Profiler) is a documented follow-up, not attempted here because no physical device is available in this environment either.
- **Face/embedding/clustering throughput numbers come from directly-inserted synthetic face rows on real photos**, not organically-ML-Kit-detected faces — this session's 3,322-photo library (2,377 old + 945 new, all synthetic/generated) again produced zero real ML-Kit-detected faces, the same persistent limitation documented since Phase 3. Detection *throughput* (photos/sec) is real and measured; detection/embedding/clustering *quality* on real human faces remains unverified in any session so far.
- **This phase's library (3,322 photos, 126MB) is smaller than Phase 6/7's dedicated 10,000-photo scale test** — Phase 12 deliberately reused/extended the existing on-device library rather than re-proving search/duplicate-detection scaling already established there; this phase's contribution is realistic-resolution decode/detection/embedding throughput, which Phase 6/7's smaller/tinier-image tests didn't measure.
- **No batch-processing/concurrency change was made to face detection or embedding generation** (both remain strictly sequential, one photo/face at a time) — introducing concurrent ML Kit/TFLite inference without evidence it's safe (thread-safety of the reused client/interpreter, memory pressure from multiple decoded bitmaps alive at once) would itself be an unverified, potentially risky change; flagged as a possible follow-up for a future phase with the tooling to safely verify it, not attempted here.

### Phase 13: Final portfolio-grade review
**Status:** Done

**Deliverable:** Senior/staff-level engineering review covering: architecture, code quality, Android architecture, ML pipeline, face-recognition accuracy risks, vector search, database design, background processing, AI orchestration, tool-calling architecture, security, privacy, permission handling, error handling, performance, battery usage, memory usage, offline capability, observability, maintainability (test coverage dropped per Global Constraints). For every significant issue: explain why it matters, rate severity, propose a solution, implement the fix if safe to do so. Produce: architecture documentation, setup instructions, local model installation instructions, developer documentation, performance benchmark results, privacy documentation, known limitations, future roadmap. Finish with a portfolio-quality README — no unsupported claims about AI accuracy or offline operation.

**What was built:** A full code-level review (not a generic checklist) across all 21 requested dimensions, documented in the new [ENGINEERING_REVIEW.md](../../ENGINEERING_REVIEW.md). Real findings, each rated by severity: (1) **High** — every multi-step Room write across `PersonRepositoryImpl` (merge/split/mark-incorrect/cluster-assignment), `OrganizationRepositoryImpl.savePlan`, and `AlbumRepositoryImpl.createAlbum` was non-transactional (2–4 separate `suspend` DAO calls with no atomicity) — a real, reachable process-death/crash-mid-write risk, not theoretical; fixed by wrapping each in `AppDatabase.withTransaction { }`, no schema change. (2) The Phase 12 hashing bug and (3) the Phase 11 logging leak are cross-referenced here as the review's own worked examples of the same class of systemic-pattern issue. Two further real gaps were found and **documented rather than fixed**, with rationale: no automated Room migration test coverage (would require test infrastructure beyond this project's standing "plain-Kotlin business logic only" testing scope), and no CI check preventing a future logging leak like finding 3. A complete documentation set was produced: [PERFORMANCE.md](../../PERFORMANCE.md), [PRIVACY.md](../../PRIVACY.md), [TESTING.md](../../TESTING.md), [DEVELOPMENT.md](../../DEVELOPMENT.md), and [MODEL_SETUP.md](../../MODEL_SETUP.md) — consolidating and formalizing what had previously only existed scattered across per-phase plan-doc entries and in-app UI — plus a rewritten, portfolio-framed [README.md](../../../README.md) explicitly presenting the project as an on-device-AI/computer-vision/vector-search/local-LLM-tool-calling/privacy-first-architecture example, with every claim traceable to a verification already on record rather than asserted fresh.

**Verification performed:**
- The three transaction fixes were verified **live, on-device** (this session's environment had a working AVD, unlike Phases 10/11 — see Phase 12's own test-setup note), not just by inspection: split and merge were both exercised through the real People UI against the same 300-face test person from Phase 12's profiling run, with the database pulled and inspected before and after each (including the `-wal` file — a pull that omits it shows stale pre-commit state and looks like a bug when there isn't one, a real gotcha hit and resolved mid-verification). A full "Organize my screenshots" NL round trip (27 proposed operations) completed cleanly through the now-transactional `savePlan` with no `SQLiteConstraintException`.
- `./gradlew :domain:test :core:common:test :tools:test :llm:orchestration:test :fsops:test` — 166/166 tests passed, no regressions from the transaction wrapping (no test needed updating, since Room-layer code is outside this project's unit-test scope by design — see [TESTING.md](../../TESTING.md)).
- `./gradlew :app:assembleDebug` — BUILD SUCCESSFUL.

**Known limitations (non-blocking):** the review itself catalogues its own remaining known limitations exhaustively (see [ENGINEERING_REVIEW.md](../../ENGINEERING_REVIEW.md)'s Known Limitations and Future Roadmap sections) rather than repeating them here — the single most load-bearing one, repeated one final time for anyone who only reads this plan doc: **no real human face photo has been available in any session across all 13 phases**, so face/embedding/clustering *accuracy* remains unvalidated; only pipeline *mechanics* have real, repeated on-device verification.
